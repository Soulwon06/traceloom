# TraceLoom Changelog

## TraceLoom V1

当前版本以本地优先的可读性运行时观测为核心，包含：

- TraceLoom 统一产品身份与 `traceloom` 运行时命名空间
- 面向 AI 应用的 Readable Observability
- 中文优先的双语 Dashboard 界面
- 术语解释组件（TermExplanation）与渐进式信息展开（Progressive Disclosure）
- HTTP 请求、响应、日志和异常的可读详情
- LLM provider 识别与 LLM Detail 视图
- Stream / Reader 查看模式
- Python client 与 `traceloom run` 命令
- FastAPI backend、SQLite 存储和本地 JSON API
- Trace / hierarchy 运行时层级关系
- Provider normalization
- Windows CLI 兼容性
- 文档、示例、Skills 和完整质量验证

本文件只记录 TraceLoom 当前仓库的版本，不把上游项目的历史版本改写为 TraceLoom 版本。
